[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "jaxzero"
version = "0.1.0"
dependencies = [
    "jax",
    "flax",
    "optax",
    "chex",
    "numpy",
    "pytest",
]

[tool.setuptools.packages.find]
where = ["."]
